import React from 'react';
import PropTypes from 'prop-types';

const RestaurantViewOrders = (props) => {
  return <div> View orders made by customers </div>;
};

RestaurantViewOrders.propTypes = {};

export default RestaurantViewOrders;
