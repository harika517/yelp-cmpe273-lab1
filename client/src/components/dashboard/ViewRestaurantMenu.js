import React from 'react';
import PropTypes from 'prop-types';

const ViewRestaurantMenu = (props) => {
  return <div></div>;
};

ViewRestaurantMenu.propTypes = {};

export default ViewRestaurantMenu;
